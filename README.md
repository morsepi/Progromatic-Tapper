# Progromatic-Tapper
Worm Petri Tapper

Requires Python

To get started run the following command once you have confirmed python is properly installed.

pip install -r requirements.txt

Once that has completed run the following command

python3 worm.py


You will have the ability to pause the tapper with the pause button and adjust the variables that are important to you.  

